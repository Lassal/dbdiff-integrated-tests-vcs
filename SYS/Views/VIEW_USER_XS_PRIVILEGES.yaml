schema: SYS
name: USER_XS_PRIVILEGES
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    name,
    security_class,
    description
  from
    sys.dba_xs_privileges
  where
    security_class_owner = sys_context('USERENV', 'CURRENT_USER')
