schema: SYS
name: RULE_EXPRESSION_CONDITIONS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    iee_id,
    or_id,
    condition_id,
    condition,
    condition_type,
    folded,
    is_cached,
    evaluate_count,
    memused
  from
    x$iee_condition
columns: []
referencedTables: []
