schema: SYS
name: USER_DBFS_HS
updatedAllowed: true
insertAllowed: true
columns:
- name: STORENAME
  dataType: VARCHAR2
  textMaxLength: 256
  nullable: true
  ordinalPosition: 1
  dataTypeLength: 256
referencedTables:
- schema: SYS
  name: DBA_DBFS_HS
viewDefinition: |-
  SELECT
    storeName
  FROM
    DBA_DBFS_HS
  WHERE
    storeOwner = sys_context('USERENV', 'SESSION_USER')
