schema: SYS
name: USER_SOURCE
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    NAME,
    TYPE,
    LINE,
    TEXT,
    ORIGIN_CON_ID
  from
    NO_ROOT_SW_FOR_LOCAL(INT $DBA_SOURCE)
  where
    OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
