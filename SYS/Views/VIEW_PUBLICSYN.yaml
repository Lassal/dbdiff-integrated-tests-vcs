schema: SYS
name: PUBLICSYN
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    sname,
    creator,
    tname,
    database,
    tabtype
  from
    synonyms
  where
    syntype = 'PUBLIC'
