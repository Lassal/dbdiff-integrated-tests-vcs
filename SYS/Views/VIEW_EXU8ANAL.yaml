schema: SYS
name: EXU8ANAL
updatedAllowed: true
insertAllowed: true
columns:
- name: ID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 1
  dataTypeLength:
- name: ROWCNT
  dataType: NUMBER
  nullable: true
  ordinalPosition: 2
  dataTypeLength:
referencedTables:
- schema: SYS
  name: TAB$
viewDefinition: |-
  SELECT
    obj#,
    SIGN(NVL(rowcnt, -1))
  FROM
    sys.tab$
