schema: SYS
name: LOADER_SKIP_UNUSABLE_INDEXES
updatedAllowed: true
insertAllowed: true
columns:
- name: VALUE
  dataType: NUMBER
  nullable: true
  ordinalPosition: 1
  dataTypeLength:
referencedTables:
- schema: SYS
  name: V$PARAMETER
viewDefinition: |-
  select
    count(*) as value
  from
    v$parameter
  where
    upper(name) = 'SKIP_UNUSABLE_INDEXES'
    and value = 'TRUE'
