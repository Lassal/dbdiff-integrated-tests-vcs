schema: SYS
name: USER_POLICY_GROUPS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    OBJECT_NAME,
    POLICY_GROUP
  FROM
    DBA_POLICY_GROUPS
  WHERE
    OBJECT_OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
columns: []
referencedTables: []
