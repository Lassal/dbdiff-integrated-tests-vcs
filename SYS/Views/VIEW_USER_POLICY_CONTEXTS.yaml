schema: SYS
name: USER_POLICY_CONTEXTS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    OBJECT_NAME,
    NAMESPACE,
    ATTRIBUTE
  FROM
    DBA_POLICY_CONTEXTS
  WHERE
    OBJECT_OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
columns: []
referencedTables: []
