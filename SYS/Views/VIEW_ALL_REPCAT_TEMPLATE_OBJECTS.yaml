schema: SYS
name: ALL_REPCAT_TEMPLATE_OBJECTS
updatedAllowed: true
insertAllowed: true
columns:
- name: DDL_NUM
  dataType: NUMBER
  nullable: false
  ordinalPosition: 4
  dataTypeLength:
- name: DDL_TEXT
  dataType: CLOB
  nullable: true
  ordinalPosition: 5
  dataTypeLength:
- name: DERIVED_FROM_ONAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: true
  ordinalPosition: 8
  dataTypeLength: 128
- name: DERIVED_FROM_SNAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: true
  ordinalPosition: 7
  dataTypeLength: 128
- name: FLAVOR_ID
  dataType: NUMBER
  nullable: true
  ordinalPosition: 9
  dataTypeLength:
- name: MASTER_ROLLBACK_SEGMENT
  dataType: VARCHAR2
  textMaxLength: 30
  nullable: true
  ordinalPosition: 6
  dataTypeLength: 30
- name: OBJECT_NAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: false
  ordinalPosition: 2
  dataTypeLength: 128
- name: OBJECT_TYPE
  dataType: VARCHAR2
  textMaxLength: 200
  nullable: true
  ordinalPosition: 3
  dataTypeLength: 200
- name: REFRESH_TEMPLATE_NAME
  dataType: VARCHAR2
  textMaxLength: 30
  nullable: false
  ordinalPosition: 1
  dataTypeLength: 30
referencedTables:
- schema: SYS
  name: ALL_USERS
- schema: SYS
  name: USER$
- schema: SYS
  name: V$ENABLEDPRIVS
- schema: SYSTEM
  name: REPCAT$_OBJECT_TYPES
- schema: SYSTEM
  name: REPCAT$_REFRESH_TEMPLATES
- schema: SYSTEM
  name: REPCAT$_TEMPLATE_OBJECTS
- schema: SYSTEM
  name: REPCAT$_TEMPLATE_TYPES
- schema: SYSTEM
  name: REPCAT$_USER_AUTHORIZATIONS
viewDefinition: |-
  select
    rt.refresh_template_name,
    t.object_name,
    ot.object_type_name object_type,
    t.ddl_num,
    t.ddl_text,
    t.master_rollback_seg,
    t.derived_from_sname,
    t.derived_from_oname,
    t.flavor_id
  from
    system.repcat$_refresh_templates rt,
    system.repcat$_template_objects t,
    system.repcat$_object_types ot,
    system.repcat$_template_types tt
  where
    t.refresh_template_id = rt.refresh_template_id
    and ot.object_type_id = t.object_type
    and rt.template_type_id = tt.template_type_id
    and bitand(rawtohex(tt.flags), 1) = 1
    and rt.refresh_template_id in (
      select
        rt.refresh_template_id
      from
        system.repcat$_refresh_templates
      where
        public_template = 'Y'
      union
      select
        rt.refresh_template_id
      from
        system.repcat$_refresh_templates rt,
        system.repcat$_user_authorizations at,
        sys.all_users au
      where
        at .refresh_template_id = rt.refresh_template_id
        and au.user_id = at .user_id
        and nvl(rt.public_template, 'N') = 'N'
        and au.user_id = userenv('SCHEMAID')
      union
      select
        rt.refresh_template_id
      from
        system.repcat$_refresh_templates rt
      where
        nvl(rt.public_template, 'N') = 'N'
        and exists (
          select
            1
          from
            v$enabledprivs
          where
            priv_number in (
              -174
              /* alter any snapshot */
            )
        )
      union
      select
        rt.refresh_template_id
      from
        system.repcat$_refresh_templates rt,
        sys.user$ u
      where
        nvl(rt.public_template, 'N') = 'N'
        and rt.owner = u.name
        and u.user# = userenv('SCHEMAID')
    )
