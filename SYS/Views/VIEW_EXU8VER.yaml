schema: SYS
name: EXU8VER
updatedAllowed: true
insertAllowed: true
columns:
- name: VERSION
  dataType: NUMBER
  nullable: true
  ordinalPosition: 1
  dataTypeLength:
referencedTables:
- schema: SYS
  name: PROPS$
viewDefinition: |-
  SELECT
    TO_NUMBER(value $)
  FROM
    sys.props$
  WHERE
    name = 'EXPORT_VIEWS_VERSION'
