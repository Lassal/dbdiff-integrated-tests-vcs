schema: SYS
name: EXU9TNE
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    ts#,
    segfile#,
    segblock#,
    length
  FROM
    sys.uet$
  WHERE
    ext# = 1
columns: []
referencedTables: []
