schema: SYS
name: EXU9TNE
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    ts#,
    segfile#,
    segblock#,
    length
  FROM
    sys.uet$
  WHERE
    ext# = 1
