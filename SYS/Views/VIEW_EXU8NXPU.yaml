schema: SYS
name: EXU8NXPU
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    owner,
    name,
    type
  FROM
    sys.exu8nxp
  WHERE
    ownerid = userenv('SCHEMAID')
