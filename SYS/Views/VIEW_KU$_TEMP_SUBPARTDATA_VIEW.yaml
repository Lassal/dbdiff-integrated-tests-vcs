schema: SYS
name: KU$_TEMP_SUBPARTDATA_VIEW
updatedAllowed: true
insertAllowed: true
columns:
- name: DSPBND
  dataType: BLOB
  nullable: true
  ordinalPosition: 8
  dataTypeLength:
- name: DSPTS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 3
  dataTypeLength:
- name: OBJ_NUM
  dataType: NUMBER
  nullable: true
  ordinalPosition: 1
  dataTypeLength:
- name: PDEFTS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 4
  dataTypeLength:
- name: SPBND
  dataType: BLOB
  nullable: true
  ordinalPosition: 7
  dataTypeLength:
- name: SPTS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 2
  dataTypeLength:
- name: TDEFTS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 5
  dataTypeLength:
- name: UDEFTS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 6
  dataTypeLength:
referencedTables:
- schema: SYS
  name: DEFSUBPART$
- schema: SYS
  name: KU$_TEMP_SUBPART_VIEW
- schema: SYS
  name: OBJ$
- schema: SYS
  name: PARTOBJ$
- schema: SYS
  name: TABCOMPART$
- schema: SYS
  name: USER$
viewDefinition: |-
  SELECT
    p.obj#,
    sp.ts_num,
    dsp.ts#,
    p.defts#,
    tpo.defts#,
    u.datats#,
    sp.bhiboundval,
    dsp.bhiboundval
  FROM
    sys.tabcompart$ p,
    sys.partobj$ tpo,
    ku$_temp_subpart_view sp,
    sys.defsubpart$ dsp,
    sys.obj$ po,
    sys.obj$ spo,
    sys.user$ u
  WHERE
    p.bo# = tpo.obj#
    AND p.subpartcnt = MOD(TRUNC(tpo.spare2 / 65536), 65536)
    AND sp.pobj_num = p.obj#
    AND po.obj# = p.obj#
    AND spo.obj# = sp.obj_num
    AND sp.subpartno = dsp.spart_position
    AND dsp.bo# = p.bo#
    AND u.user# = po.owner#
    AND (
      spo.subname = (po.subname || '_' || dsp.spart_name)
      OR (
        po.subname LIKE 'SYS_P%'
        AND spo.subname LIKE 'SYS_SUBP%'
      )
    )
