schema: SYS
name: USER_SR_OBJ
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    NAME,
    TYPE,
    GROUP_ID,
    STAGING_LOG_NAME
  from
    dba_sr_obj
  where
    owner = SYS_CONTEXT ('USERENV', 'SESSION_USER')
columns: []
referencedTables: []
