schema: SYS
name: IMP9USR
updatedAllowed: true
insertAllowed: true
columns:
- name: NAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: false
  ordinalPosition: 1
  dataTypeLength: 128
- name: USERID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 2
  dataTypeLength:
referencedTables:
- schema: SYS
  name: USER$
viewDefinition: |-
  SELECT
    u.name,
    u.user#
  FROM
    sys.user$ u
  WHERE
    u.user# = userenv('SCHEMAID')
