schema: SYS
name: IMP9USR
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    u.name,
    u.user#
  FROM
    sys.user$ u
  WHERE
    u.user# = userenv('SCHEMAID')
