schema: SYS
name: DBA_SCHEDULER_REMOTE_DATABASES
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    database_name,
    decode(reg_status, 0, 'SOURCE', 'DESTINATION'),
    database_link
  FROM
    scheduler$_remote_dbs
