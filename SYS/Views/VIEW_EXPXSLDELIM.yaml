schema: SYS
name: EXPXSLDELIM
updatedAllowed: true
insertAllowed: true
columns:
- name: XSLDELIMITER
  dataType: VARCHAR2
  textMaxLength: 4
  nullable: true
  ordinalPosition: 1
  dataTypeLength: 4
referencedTables:
- schema: SYS
  name: METAXSLPARAM$
viewDefinition: |-
  SELECT
    SUBSTR(DEFAULT_VAL, 2, 4)
  FROM
    SYS.METAXSLPARAM$
  WHERE
    MODEL = 'ORACLE'
    AND TRANSFORM = 'PARSE'
    AND PARAM = 'PRS_DELIM'
