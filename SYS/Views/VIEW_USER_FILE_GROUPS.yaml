schema: SYS
name: USER_FILE_GROUPS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    file_group_name,
    keep_files,
    min_versions,
    max_versions,
    retention_days,
    created,
    comments,
    default_directory
  from
    "_USER_FILE_GROUPS"
columns: []
referencedTables: []
