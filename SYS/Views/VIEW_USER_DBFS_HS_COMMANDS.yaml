schema: SYS
name: USER_DBFS_HS_COMMANDS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    RC.StoreName,
    RC.StoreCommand,
    RC.StoreFlags
  FROM
    DBA_DBFS_HS_COMMANDS RC,
    USER_DBFS_HS UR
  WHERE
    UR.StoreName = RC.StoreName
columns: []
referencedTables: []
