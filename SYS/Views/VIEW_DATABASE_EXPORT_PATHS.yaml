schema: SYS
name: DATABASE_EXPORT_PATHS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    OBJECT_PATH,
    0
  from
    dba_export_paths
  where
    het_type = 'DATABASE_EXPORT'
  union
  select
    unique tag,
    1
  from
    impcalloutreg$
columns: []
referencedTables: []
