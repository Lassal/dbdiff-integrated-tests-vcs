schema: SYS
name: USER_XMLTYPE_COLS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    SCHEMA_ID,
    SCHEMA_URL,
    COL_NAME,
    QUALIFIED_COL_NAME,
    TABLE_NAME
  from
    DBA_XMLTYPE_COLS
  where
    TABLE_OWNER = USER
columns: []
referencedTables: []
