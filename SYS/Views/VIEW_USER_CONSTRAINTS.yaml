schema: SYS
name: USER_CONSTRAINTS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    OWNER,
    CONSTRAINT_NAME,
    CONSTRAINT_TYPE,
    TABLE_NAME,
    SEARCH_CONDITION,
    SEARCH_CONDITION_VC,
    R_OWNER,
    R_CONSTRAINT_NAME,
    DELETE_RULE,
    STATUS,
    DEFERRABLE,
    DEFERRED,
    VALIDATED,
    GENERATED,
    BAD,
    RELY,
    LAST_CHANGE,
    INDEX_OWNER,
    INDEX_NAME,
    INVALID,
    VIEW_RELATED,
    ORIGIN_CON_ID
  from
    NO_ROOT_SW_FOR_LOCAL(INT $INT$DBA_CONSTRAINTS) INT $INT$DBA_CONSTRAINTS
  where
    INT $INT$DBA_CONSTRAINTS.OBJECT_TYPE# = 4
    /* views */
    and INT $INT$DBA_CONSTRAINTS.OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
  union all
  select
    OWNER,
    CONSTRAINT_NAME,
    CONSTRAINT_TYPE,
    TABLE_NAME,
    SEARCH_CONDITION,
    SEARCH_CONDITION_VC,
    R_OWNER,
    R_CONSTRAINT_NAME,
    DELETE_RULE,
    STATUS,
    DEFERRABLE,
    DEFERRED,
    VALIDATED,
    GENERATED,
    BAD,
    RELY,
    LAST_CHANGE,
    INDEX_OWNER,
    INDEX_NAME,
    INVALID,
    VIEW_RELATED,
    ORIGIN_CON_ID
  from
    NO_COMMON_DATA(INT $INT$DBA_CONSTRAINTS) INT $INT$DBA_CONSTRAINTS
  where
    INT $INT$DBA_CONSTRAINTS.OBJECT_TYPE# = 2
    /* tables */
    and INT $INT$DBA_CONSTRAINTS.OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
columns: []
referencedTables: []
