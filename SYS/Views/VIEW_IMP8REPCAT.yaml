schema: SYS
name: IMP8REPCAT
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    name,
    type #
  FROM
    sys.obj$
  WHERE
    name IN ('DBMS_SNAPSHOT_UTL', 'DBMS_REPCAT_MIG')
    AND type # = 11
    AND owner# = 0
columns: []
referencedTables: []
