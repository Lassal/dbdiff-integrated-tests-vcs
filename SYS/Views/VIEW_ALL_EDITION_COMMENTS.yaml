schema: SYS
name: ALL_EDITION_COMMENTS
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    o.name,
    c .comment$
  from
    sys.obj$ o,
    sys.com$ c
  where
    o.obj# = c .obj# (+)
    and o.type# = 57
