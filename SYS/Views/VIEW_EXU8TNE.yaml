schema: SYS
name: EXU8TNE
updatedAllowed: true
insertAllowed: true
columns:
- name: BLOCKNO
  dataType: NUMBER
  nullable: false
  ordinalPosition: 3
  dataTypeLength:
- name: FILENO
  dataType: NUMBER
  nullable: false
  ordinalPosition: 2
  dataTypeLength:
- name: LENGTH
  dataType: NUMBER
  nullable: true
  ordinalPosition: 4
  dataTypeLength:
- name: TSNO
  dataType: NUMBER
  nullable: false
  ordinalPosition: 1
  dataTypeLength:
referencedTables:
- schema: SYS
  name: EXU9TNE
- schema: SYS
  name: TS$
viewDefinition: |-
  SELECT
    e.tsno,
    e.fileno,
    e.blockno,
    CEIL(
      e.length * (
        (
          SELECT
            t1$.blocksize
          FROM
            sys.ts$ t1$
          WHERE
            t1$.ts# = e.tsno
        ) / (
          SELECT
            t0$.blocksize
          FROM
            sys.ts$ t0$
          WHERE
            t0$.ts# = 0
        )
      )
    )
  FROM
    sys.exu9tne e
