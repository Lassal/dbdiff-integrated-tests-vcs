schema: SYS
name: USER_CLUSTERING_DIMENSIONS
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    d.TABLE_NAME,
    d.DIMENSION_OWNER,
    d.DIMENSION_NAME
  from
    DBA_CLUSTERING_DIMENSIONS d
  WHERE
    d.OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
