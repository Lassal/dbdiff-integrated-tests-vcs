schema: SYS
name: ALL_POLICY_ATTRIBUTES
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    OBJECT_OWNER,
    OBJECT_NAME,
    POLICY_GROUP,
    POLICY_NAME,
    NAMESPACE,
    ATTRIBUTE
  FROM
    DBA_POLICY_ATTRIBUTES,
    ALL_TABLES t
  WHERE
    (
      OBJECT_OWNER = t.OWNER
      AND OBJECT_NAME = t.TABLE_NAME
    )
  union all
  SELECT
    OBJECT_OWNER,
    OBJECT_NAME,
    POLICY_GROUP,
    POLICY_NAME,
    NAMESPACE,
    ATTRIBUTE
  FROM
    DBA_POLICY_ATTRIBUTES,
    ALL_VIEWS v
  WHERE
    (
      OBJECT_OWNER = v.OWNER
      AND OBJECT_NAME = v.VIEW_NAME
    )
  union all
  SELECT
    OBJECT_OWNER,
    OBJECT_NAME,
    POLICY_GROUP,
    POLICY_NAME,
    NAMESPACE,
    ATTRIBUTE
  FROM
    DBA_POLICY_ATTRIBUTES,
    ALL_SYNONYMS s
  WHERE
    (
      OBJECT_OWNER = s.OWNER
      AND OBJECT_NAME = s.SYNONYM_NAME
    )
