schema: SYS
name: USER_ENCRYPTED_COLUMNS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    TABLE_NAME,
    COLUMN_NAME,
    ENCRYPTION_ALG,
    SALT,
    INTEGRITY_ALG
  from
    DBA_ENCRYPTED_COLUMNS
  where
    OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
columns: []
referencedTables: []
