schema: SYS
name: USER_SR_OBJ_STATUS_ALL
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    NAME,
    TYPE,
    GROUP_ID,
    STATUS,
    CURRENT_RUN,
    CURRENT_GROUP,
    ERROR_NUMBER,
    ERROR_MESSAGE,
    LAST_MODIFIED_TIME
  from
    dba_sr_obj_status_all
  where
    owner = SYS_CONTEXT ('USERENV', 'SESSION_USER')
columns: []
referencedTables: []
