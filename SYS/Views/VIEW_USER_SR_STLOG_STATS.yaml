schema: SYS
name: USER_SR_STLOG_STATS
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    TABLE_NAME,
    STAGING_LOG_NAME,
    NUM_INSERTS,
    NUM_DELETES,
    NUM_UPDATES,
    PSL_MODE
  from
    DBA_SR_STLOG_STATS
  where
    owner = SYS_CONTEXT ('USERENV', 'SESSION_USER')
