schema: SYS
name: USER_DBFS_HS_PROPERTIES
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    RP.storeName,
    RP.propertyName,
    RP.propertyValue
  FROM
    DBA_DBFS_HS_PROPERTIES RP,
    USER_DBFS_HS UR
  WHERE
    UR.storeName = RP.storeName
columns: []
referencedTables: []
