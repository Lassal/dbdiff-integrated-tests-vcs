schema: SYS
name: ALL_SCHEDULER_GROUPS
updatedAllowed: true
insertAllowed: true
columns:
- name: COMMENTS
  dataType: VARCHAR2
  textMaxLength: 240
  nullable: true
  ordinalPosition: 6
  dataTypeLength: 240
- name: ENABLED
  dataType: VARCHAR2
  textMaxLength: 5
  nullable: true
  ordinalPosition: 4
  dataTypeLength: 5
- name: GROUP_NAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: false
  ordinalPosition: 2
  dataTypeLength: 128
- name: GROUP_TYPE
  dataType: VARCHAR2
  textMaxLength: 13
  nullable: true
  ordinalPosition: 3
  dataTypeLength: 13
- name: NUMBER_OF_MEMBERS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 5
  dataTypeLength:
- name: OWNER
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: false
  ordinalPosition: 1
  dataTypeLength: 128
referencedTables:
- schema: SYS
  name: ALL_SCHEDULER_GROUP_MEMBERS
- schema: SYS
  name: OBJ$
- schema: SYS
  name: OBJAUTH$
- schema: SYS
  name: SCHEDULER$_WINDOW_GROUP
- schema: SYS
  name: USER$
- schema: SYS
  name: V$ENABLEDPRIVS
- schema: SYS
  name: X$KZSRO
viewDefinition: |-
  SELECT
    u.name,
    o.name,
    decode(
      bitand(w.flags, 8 + 16),
      0,
      'WINDOW',
      8,
      'JOB',
      16,
      decode(
        bitand(w.flags, 4096 + 8192),
        4096,
        'DB_DEST',
        8192,
        'EXTERNAL_DEST',
        'UNKOWN_DEST'
      )
    ),
    DECODE(BITAND(w.flags, 1), 0, 'FALSE', 1, 'TRUE'),
    (
      SELECT
        COUNT(*)
      FROM
        all_scheduler_group_members agm
      WHERE
        agm.group_name = o.name
        and agm.owner = u.name
    ),
    w.comments
  FROM
    obj$ o,
    user $ u,
    scheduler$_window_group w
  WHERE
    o.owner# = u.user#
    AND o.obj# = w.obj#
    AND (
      bitand(w.flags, 8 + 16) = 0 -- window group
      or o.owner# = userenv('SCHEMAID') -- user is owner
      or o.obj# in -- user has obj privs on group
      (
        select
          oa.obj#
        from
          sys.objauth$ oa
        where
          grantee# in (
            select
              kzsrorol
            from
              x$kzsro
          )
      )
      or
      /* user has create any job, except for SYS group */
      (
        exists (
          select
            null
          from
            v$enabledprivs
          where
            priv_number in (
              -265
              /* CREATE ANY JOB */
            )
        )
        and o.owner# != 0
      )
    )
