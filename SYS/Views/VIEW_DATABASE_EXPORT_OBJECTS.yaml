schema: SYS
name: DATABASE_EXPORT_OBJECTS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    OBJECT_PATH,
    COMMENTS,
    NAMED
  from
    dba_export_objects
  where
    het_type = 'DATABASE_EXPORT'
  union
  select
    unique TAG,
    CMNT,
    NULL
  from
    impcalloutreg$
  where
    cmnt is not null
columns: []
referencedTables: []
