schema: SYS
name: KU$_COLLECTION_VIEW
updatedAllowed: true
insertAllowed: true
columns:
- name: CHARSETFORM
  dataType: NUMBER
  nullable: true
  ordinalPosition: 10
  dataTypeLength:
- name: CHARSETID
  dataType: NUMBER
  nullable: true
  ordinalPosition: 9
  dataTypeLength:
- name: COLL_TOID
  dataType: RAW
  nullable: true
  ordinalPosition: 3
  dataTypeLength:
- name: COLL_TYPE
  dataType: KU$_SIMPLE_TYPE_T
  nullable: true
  ordinalPosition: 18
  dataTypeLength:
- name: COLL_VERSION
  dataType: NUMBER
  nullable: true
  ordinalPosition: 4
  dataTypeLength:
- name: ELEM_TOID
  dataType: RAW
  nullable: true
  ordinalPosition: 5
  dataTypeLength:
- name: ELEM_VERSION
  dataType: NUMBER
  nullable: true
  ordinalPosition: 6
  dataTypeLength:
- name: LENGTH
  dataType: NUMBER
  nullable: true
  ordinalPosition: 11
  dataTypeLength:
- name: PRECISION
  dataType: NUMBER
  nullable: true
  ordinalPosition: 12
  dataTypeLength:
- name: PROPERTIES
  dataType: NUMBER
  nullable: true
  ordinalPosition: 8
  dataTypeLength:
- name: SCALE
  dataType: NUMBER
  nullable: true
  ordinalPosition: 13
  dataTypeLength:
- name: SPARE1
  dataType: NUMBER
  nullable: true
  ordinalPosition: 15
  dataTypeLength:
- name: SPARE2
  dataType: NUMBER
  nullable: true
  ordinalPosition: 16
  dataTypeLength:
- name: SPARE3
  dataType: NUMBER
  nullable: true
  ordinalPosition: 17
  dataTypeLength:
- name: SYNOBJ
  dataType: NUMBER
  nullable: true
  ordinalPosition: 7
  dataTypeLength:
- name: TOID
  dataType: RAW
  nullable: true
  ordinalPosition: 1
  dataTypeLength:
- name: TYPEMD
  dataType: KU$_SIMPLE_TYPE_T
  nullable: true
  ordinalPosition: 19
  dataTypeLength:
- name: UPPER_BOUND
  dataType: NUMBER
  nullable: true
  ordinalPosition: 14
  dataTypeLength:
- name: VERSION
  dataType: NUMBER
  nullable: true
  ordinalPosition: 2
  dataTypeLength:
referencedTables:
- schema: SYS
  name: COLLECTION$
- schema: SYS
  name: KU$_SIMPLE_TYPE_VIEW
viewDefinition: |-
  select
    toid,
    /* TOID */
    version#,
    /* internal type version number */
    coll_toid,
    /* collection TOID (TABLE, VARRAY, etc.) */
    coll_version#,
    /* collection type's internal version number */
    elem_toid,
    /* element's TOID */
    elem_version#,
    /* element's type's internal version number */
    synobj#,
    /* obj# of type synonym */
    properties,
    /* element's properties: */
    charsetid,
    /* character set id */
    charsetform,
    /* character set form */
    length,
    /* fixed character string length or */
    /* maximum varying character string length */
    precision,
    /* fixed- or floating-point numeric precision */
    scale,
    /* fixed-point numeric scale */
    upper_bound,
    /* fixed array size or varying array upper bound */
    spare1,
    /* fractional seconds precision */
    spare2,
    /* interval leading field precision */
    spare3,
    (
      select
        value(st)
      from
        ku$_simple_type_View st
      where
        st.toid = c .coll_toid
        and st.version = (
          SELECT
            MAX(ST2.VERSION)
          FROM
            ku$_simple_type_View st2
          WHERE
            ST2.TOID = c .coll_toid
        )
    ),
    (
      select
        value(st)
      from
        ku$_simple_type_View st
      where
        st.toid = c .elem_toid
        and st.version = (
          SELECT
            MAX(ST2.VERSION)
          FROM
            ku$_simple_type_View st2
          WHERE
            ST2.TOID = c .elem_toid
        )
    )
  FROM
    sys.collection$ c
