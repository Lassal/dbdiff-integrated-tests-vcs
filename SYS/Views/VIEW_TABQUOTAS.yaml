schema: SYS
name: TABQUOTAS
updatedAllowed: true
insertAllowed: true
columns:
- name: MAXEXT
  dataType: NUMBER
  nullable: false
  ordinalPosition: 5
  dataTypeLength:
- name: NEXTEXT
  dataType: NUMBER
  nullable: true
  ordinalPosition: 4
  dataTypeLength:
- name: OBJNO
  dataType: NUMBER
  nullable: false
  ordinalPosition: 3
  dataTypeLength:
- name: PFREE
  dataType: NUMBER
  nullable: true
  ordinalPosition: 7
  dataTypeLength:
- name: PINC
  dataType: NUMBER
  nullable: true
  ordinalPosition: 6
  dataTypeLength:
- name: PUSED
  dataType: NUMBER
  nullable: true
  ordinalPosition: 8
  dataTypeLength:
- name: TNAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: false
  ordinalPosition: 1
  dataTypeLength: 128
- name: TYPE
  dataType: VARCHAR2
  textMaxLength: 7
  nullable: true
  ordinalPosition: 2
  dataTypeLength: 7
referencedTables:
- schema: SYS
  name: OBJ$
- schema: SYS
  name: SEG$
- schema: SYS
  name: SYSSEGOBJ
- schema: SYS
  name: TS$
viewDefinition: |-
  select
    t.name,
    so.type,
    t.obj#,
    decode(
      bitand(ts.flags, 3),
      1,
      to_number(NULL),
      s.extsize * ts.blocksize
    ),
    s.maxexts,
    decode(
      bitand(ts.flags, 3),
      1,
      to_number(NULL),
      s.extpct
    ),
    so.pctfree$,
    decode(
      bitand(ts.flags, 32),
      32,
      to_number(NULL),
      so.pctused$
    )
  from
    sys.ts$ ts,
    sys.seg$ s,
    sys.obj$ t,
    syssegobj so
  where
    t.owner# = userenv('SCHEMAID')
    and t.obj# = so.obj#
    and so.file# = s.file#
    and so.block# = s.block#
    and s.ts# = ts.ts#
