schema: SYS
name: ALL_FILE_GROUPS
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    file_group_owner,
    file_group_name,
    keep_files,
    min_versions,
    max_versions,
    retention_days,
    created,
    comments,
    default_directory
  from
    "_ALL_FILE_GROUPS"
