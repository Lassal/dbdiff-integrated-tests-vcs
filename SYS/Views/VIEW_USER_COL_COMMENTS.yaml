schema: SYS
name: USER_COL_COMMENTS
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    TABLE_NAME,
    COLUMN_NAME,
    COMMENTS,
    ORIGIN_CON_ID
  from
    NO_ROOT_SW_FOR_LOCAL(INT $DBA_COL_COMMENTS)
  where
    OWNER = SYS_CONTEXT('USERENV', 'CURRENT_USER')
