schema: SYS
name: EXU9XDBUID
updatedAllowed: true
insertAllowed: true
columns:
- name: XDB_UID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 1
  dataTypeLength:
referencedTables:
- schema: SYS
  name: REGISTRY$
viewDefinition: |-
  SELECT
    r$.schema#
  FROM
    sys.registry$ r$
  WHERE
    r$.cid = 'XDB'
