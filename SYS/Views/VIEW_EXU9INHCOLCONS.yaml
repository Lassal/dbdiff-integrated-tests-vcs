schema: SYS
name: EXU9INHCOLCONS
updatedAllowed: true
insertAllowed: true
columns:
- name: CNAME
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 3
  dataTypeLength: 4000
- name: INTCOLID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 2
  dataTypeLength:
- name: SUBTYPEFLAGS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 5
  dataTypeLength:
- name: TOBJID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 1
  dataTypeLength:
- name: TYPEFLAGS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 4
  dataTypeLength:
- name: TYPENAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: true
  ordinalPosition: 8
  dataTypeLength: 128
- name: TYPEOWNER
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: true
  ordinalPosition: 7
  dataTypeLength: 128
- name: TYPEOWNERID
  dataType: NUMBER
  nullable: true
  ordinalPosition: 6
  dataTypeLength:
referencedTables:
- schema: SYS
  name: ATTRCOL$
- schema: SYS
  name: COL$
- schema: SYS
  name: COLTYPE$
- schema: SYS
  name: OBJ$
- schema: SYS
  name: SESSION_ROLES
- schema: SYS
  name: SUBCOLTYPE$
- schema: SYS
  name: USER$
viewDefinition: |-
  SELECT
    ct$.obj#,
    ct$.intcol#,
    DECODE(
      BITAND(c $.property, 1),
      0,
      '"' || c $.name || '"',
      1,
      ac$.name
    ),
    ct$.flags,
    NVL(sc$.flags, 0),
    NVL(so$.owner#, 0),
    NVL(su$.name, ' '),
    NVL(so$.name, ' ')
  FROM
    sys.coltype$ ct$,
    sys.col$ c $,
    sys.attrcol$ ac$,
    sys.subcoltype$ sc$,
    sys.obj$ so$,
    sys.user$ su$,
    sys.obj$ to $
  WHERE
    bitand (ct$.flags, (512 + 1024 + 2048 + 4096)) != 0
    AND ct$.obj# = c $.obj#
    AND ct$.intcol# = c $.intcol#
    AND ct$.obj# = ac$.obj# (+)
    AND ct$.intcol# = ac$.intcol# (+)
    AND ct$.obj# = sc$.obj# (+)
    AND ct$.intcol# = sc$.intcol# (+)
    AND bitand(NVL(sc$.flags, 1), 1 + 2) != 0
    AND sc$.toid = so$.oid$ (+)
    AND so$.owner# = su$.user# (+)
    AND ct$.obj# = to $.obj#
    AND (
      userenv('SCHEMAID') = 0
      OR (userenv('SCHEMAID') = to $.owner#)
      OR EXISTS(
        SELECT
          *
        FROM
          sys.session_roles
        WHERE
          role = 'SELECT_CATALOG_ROLE'
      )
    )
