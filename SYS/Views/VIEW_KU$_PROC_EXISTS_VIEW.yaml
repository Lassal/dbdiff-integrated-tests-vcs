schema: SYS
name: KU$_PROC_EXISTS_VIEW
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    o.obj#,
    u.name,
    o.type#
  from
    obj$ o,
    user $ u
  where
    o.owner# = u.user#
    and (
      SYS_CONTEXT('USERENV', 'CURRENT_USERID') IN (o.owner#, 0)
      OR EXISTS (
        SELECT
          *
        FROM
          sys.session_roles
        WHERE
          role = 'SELECT_CATALOG_ROLE'
      )
    )
columns: []
referencedTables: []
