schema: SYS
name: USER_RULE_SETS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    /*+ all_rows */
    o.name,
    bu.name,
    bo.name,
    r.rs_comment
  FROM
    rule_set$ r,
    obj$ o,
    obj$ bo,
    user $ bu
  WHERE
    r.obj# = o.obj#
    and o.owner# = USERENV('SCHEMAID')
    and r.ectx# = bo.obj#(+)
    and bo.owner# = bu.user#(+)
columns: []
referencedTables: []
