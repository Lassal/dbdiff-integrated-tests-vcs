schema: SYS
name: DEFERRCOUNT
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    count(1) errcount,
    destination
  FROM
    deferror
  GROUP BY
    destination
