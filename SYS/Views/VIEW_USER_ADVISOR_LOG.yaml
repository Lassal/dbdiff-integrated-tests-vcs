schema: SYS
name: USER_ADVISOR_LOG
updatedAllowed: true
insertAllowed: true
columns:
- name: ACTIVITY_COUNTER
  dataType: NUMBER
  nullable: true
  ordinalPosition: 10
  dataTypeLength:
- name: ERROR_MESSAGE
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 12
  dataTypeLength: 4000
- name: EXECUTION_END
  dataType: DATE
  nullable: true
  ordinalPosition: 4
  dataTypeLength:
- name: EXECUTION_START
  dataType: DATE
  nullable: true
  ordinalPosition: 3
  dataTypeLength:
- name: METRIC_UNITS
  dataType: VARCHAR2
  textMaxLength: 64
  nullable: true
  ordinalPosition: 9
  dataTypeLength: 64
- name: PCT_COMPLETION_TIME
  dataType: NUMBER
  nullable: true
  ordinalPosition: 7
  dataTypeLength:
- name: PROGRESS_METRIC
  dataType: NUMBER
  nullable: true
  ordinalPosition: 8
  dataTypeLength:
- name: RECOMMENDATION_COUNT
  dataType: NUMBER
  nullable: true
  ordinalPosition: 11
  dataTypeLength:
- name: STATUS
  dataType: VARCHAR2
  textMaxLength: 11
  nullable: true
  ordinalPosition: 5
  dataTypeLength: 11
- name: STATUS_MESSAGE
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 6
  dataTypeLength: 4000
- name: TASK_ID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 1
  dataTypeLength:
- name: TASK_NAME
  dataType: VARCHAR2
  textMaxLength: 128
  nullable: true
  ordinalPosition: 2
  dataTypeLength: 128
referencedTables:
- schema: SYS
  name: WRI$_ADV_EXECUTIONS
- schema: SYS
  name: WRI$_ADV_TASKS
viewDefinition: |-
  select
    a .id as task_id,
    a .name as task_name,
    nvl(e.exec_start, a .exec_start) as execution_start,
    nvl(e.exec_end, a .exec_end) as execution_end,
    decode(
      nvl(e.status, a .status),
      1,
      'INITIAL',
      2,
      'EXECUTING',
      3,
      'COMPLETED',
      4,
      'INTERRUPTED',
      5,
      'CANCELLED',
      6,
      'FATAL ERROR'
    ) as status,
    dbms_advisor.format_message_group(
      nvl(e.status_msg_id, a .status_msg_id)
    ) as status_message,
    a .pct_completion_time as pct_completion_time,
    a .progress_metric as progress_metric,
    a .metric_units as metric_units,
    a .activity_counter as activity_counter,
    a .rec_count as recommendation_count,
    dbms_advisor.format_message_group(nvl(e.error_msg_id, a .error_msg#)) as error_message
  from
    wri$_adv_tasks a,
    wri$_adv_executions e
  where
    a .id = e.task_id(+)
    and a .last_exec_name = e.name(+)
    and a .advisor_id = e.advisor_id(+)
    and a .owner# = userenv('SCHEMAID')
    and bitand(a .property, 6) = 4
