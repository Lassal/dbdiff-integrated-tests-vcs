schema: SYS
name: USER_COMPARISON_COLUMNS
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    comparison_name,
    column_position,
    column_name,
    index_column
  FROM
    dba_comparison_columns
  WHERE
    owner = SYS_CONTEXT('USERENV', 'CURRENT_USER')
columns: []
referencedTables: []
