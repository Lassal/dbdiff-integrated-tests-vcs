schema: SYS
name: DBMS_FBT
routineType: PACKAGE
returnParamater: null
parameters:
- name: FBT_ANALYZE(1).TABLE_NAME
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 1
- name: FBT_ANALYZE(1).FLASHBACK_SCN
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 2
- name: FBT_ANALYZE(1).TMPTBCUR
  dataType: REF CURSOR
  parameterMode: OUT
  ordinalPosition: 3
- name: FBT_ANALYZE(2).TABLE_NAME
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 1
- name: FBT_ANALYZE(2).FLASHBACK_TIME
  dataType: TIMESTAMP
  numericPrecision: 6
  parameterMode: IN
  ordinalPosition: 2
- name: FBT_ANALYZE(2).TMPTBCUR
  dataType: REF CURSOR
  parameterMode: OUT
  ordinalPosition: 3
- name: FBT_EXECUTE(1).TABLE_NAMES
  dataType: VARRAY
  parameterMode: IN
  ordinalPosition: 1
- name: FBT_EXECUTE(1).FLASHBACK_SCN
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 2
- name: FBT_EXECUTE(2).TABLE_NAMES
  dataType: VARRAY
  parameterMode: IN
  ordinalPosition: 1
- name: FBT_EXECUTE(2).FLASHBACK_TIME
  dataType: TIMESTAMP
  numericPrecision: 6
  parameterMode: IN
  ordinalPosition: 2
routineDefinition: |-
  PACKAGE dbms_fbt AUTHID CURRENT_USER IS -- PUBLIC TYPES
  TYPE TMPTBCURTYPE IS REF CURSOR;
  -- PUBLIC PROCEDURES AND FUNCTIONS
  PROCEDURE fbt_analyze(
    table_name IN VARCHAR2,
    flashback_scn IN NUMBER,
    tmptbcur OUT TMPTBCURTYPE
  );
  PROCEDURE fbt_analyze(
    table_name IN VARCHAR2,
    flashback_time IN TIMESTAMP,
    tmptbcur OUT TMPTBCURTYPE
  );
  PROCEDURE fbt_execute(
    table_names IN FLASHBACKTBLIST,
    flashback_scn IN NUMBER
  );
  PROCEDURE fbt_execute(
    table_names IN FLASHBACKTBLIST,
    flashback_time IN TIMESTAMP
  );
  PROCEDURE fbt_discard;
  END;
