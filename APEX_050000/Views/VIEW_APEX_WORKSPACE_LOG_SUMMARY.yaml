schema: APEX_050000
name: APEX_WORKSPACE_LOG_SUMMARY
updatedAllowed: true
insertAllowed: true
columns:
- name: AGENTS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 13
  dataTypeLength:
- name: APEX_SESSIONS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 14
  dataTypeLength:
- name: APPLICATION_ID
  dataType: NUMBER
  nullable: true
  ordinalPosition: 3
  dataTypeLength:
- name: APPLICATION_NAME
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: true
  ordinalPosition: 4
  dataTypeLength: 255
- name: AVERAGE_ELAPSED_TIME
  dataType: NUMBER
  nullable: true
  ordinalPosition: 8
  dataTypeLength:
- name: CACHED_PAGE_VIEWS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 17
  dataTypeLength:
- name: DISTINCT_PAGES
  dataType: NUMBER
  nullable: true
  ordinalPosition: 6
  dataTypeLength:
- name: DYNAMIC_PAGE_VIEWS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 16
  dataTypeLength:
- name: FIRST_VIEW
  dataType: DATE
  nullable: true
  ordinalPosition: 18
  dataTypeLength:
- name: IP_ADDRESSES
  dataType: NUMBER
  nullable: true
  ordinalPosition: 12
  dataTypeLength:
- name: LAST_VIEW
  dataType: DATE
  nullable: true
  ordinalPosition: 19
  dataTypeLength:
- name: MAXIMUM_ELAPSED_TIME
  dataType: NUMBER
  nullable: true
  ordinalPosition: 10
  dataTypeLength:
- name: MINIMUM_ELAPSED_TIME
  dataType: NUMBER
  nullable: true
  ordinalPosition: 9
  dataTypeLength:
- name: PAGE_VIEWS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 5
  dataTypeLength:
- name: PAGE_VIEWS_WITH_ERRORS
  dataType: NUMBER
  nullable: true
  ordinalPosition: 15
  dataTypeLength:
- name: PERIOD_IN_DAYS
  dataType: NUMBER
  numericScale: 0
  nullable: true
  ordinalPosition: 20
  dataTypeLength:
- name: TOTAL_ELAPSED_TIME
  dataType: NUMBER
  nullable: true
  ordinalPosition: 7
  dataTypeLength:
- name: TOTAL_ROWS_QUERIED
  dataType: NUMBER
  nullable: true
  ordinalPosition: 11
  dataTypeLength:
- name: WORKSPACE
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: false
  ordinalPosition: 1
  dataTypeLength: 255
- name: WORKSPACE_DISPLAY_NAME
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 2
  dataTypeLength: 4000
referencedTables:
- schema: APEX_050000
  name: WWV_FLOWS
- schema: APEX_050000
  name: WWV_FLOW_ACTIVITY_LOG
- schema: APEX_050000
  name: WWV_FLOW_COMPANIES
- schema: APEX_050000
  name: WWV_FLOW_COMPANY_SCHEMAS
- schema: SYS
  name: DUAL
viewDefinition: |-
  select
    w.short_name workspace,
    w.display_name workspace_display_name,
    l.flow_id application_id,
    f.name application_name,
    count(*) page_views,
    count(distinct l.step_id) distinct_pages,
    sum(l.elap) total_elapsed_time,
    avg(l.elap) average_elapsed_time,
    min(l.elap) minimum_elapsed_time,
    max(l.elap) maximum_elapsed_time,
    sum(l.num_rows) total_rows_queried,
    count(distinct l.ip_address) ip_addresses,
    count(distinct l.USER_AGENT) agents,
    count(distinct l.session_id) apex_sessions,
    sum(decode(l.sqlerrm, null, 0, 1)) page_views_with_errors,
    sum(decode(l.page_mode, 'D', 1, 'C', 1, 0)) dynamic_page_views,
    sum(decode(l.page_mode, 'R', 1, 0)) cached_page_views,
    min(l.time_stamp) first_view,
    max(l.time_stamp) last_view,
    max(l.time_stamp) - min(l.time_stamp) period_in_days
  from
    wwv_flow_activity_log l,
    wwv_flows f,
    wwv_flow_companies w,
    (
      select
        nvl(v('FLOW_SECURITY_GROUP_ID'), 0) sgid,
        (
          select
            sys_context('USERENV', 'CURRENT_USER')
          from
            sys.dual
        ) cu
      from
        sys.dual
    ) d
  where
    (
      l.security_group_id in (
        select
          security_group_id
        from
          wwv_flow_company_schemas
        where
          schema = cu
      )
      or (
        select
          wwv_flow_security.has_apex_administrator_role_yn(cu)
        from
          sys.dual
      ) = 'Y'
      or d.sgid = l.security_group_id
    )
    and --
    l.security_group_id = w.PROVISIONING_COMPANY_ID
    and l.flow_id = f.id(+)
    and l.time_stamp > sysdate - 14
    and w.PROVISIONING_COMPANY_ID != 0
  group by
    w.short_name,
    w.display_name,
    l.flow_id,
    f.name
