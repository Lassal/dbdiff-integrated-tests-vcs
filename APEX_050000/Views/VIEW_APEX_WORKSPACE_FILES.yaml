schema: APEX_050000
name: APEX_WORKSPACE_FILES
updatedAllowed: true
insertAllowed: true
columns:
- name: APPLICATION_ID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 5
  dataTypeLength:
- name: APPLICATION_NAME
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: true
  ordinalPosition: 6
  dataTypeLength: 255
- name: CREATED_BY
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: true
  ordinalPosition: 10
  dataTypeLength: 255
- name: CREATED_ON
  dataType: DATE
  nullable: true
  ordinalPosition: 12
  dataTypeLength:
- name: DOCUMENT
  dataType: BLOB
  nullable: true
  ordinalPosition: 14
  dataTypeLength:
- name: EMAIL
  dataType: VARCHAR2
  textMaxLength: 240
  nullable: true
  ordinalPosition: 11
  dataTypeLength: 240
- name: FILE_ID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 4
  dataTypeLength:
- name: FILE_NAME
  dataType: VARCHAR2
  textMaxLength: 400
  nullable: true
  ordinalPosition: 7
  dataTypeLength: 400
- name: FILE_SIZE
  dataType: NUMBER
  nullable: true
  ordinalPosition: 9
  dataTypeLength:
- name: FILE_TYPE
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: true
  ordinalPosition: 13
  dataTypeLength: 255
- name: MIME_TYPE
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: true
  ordinalPosition: 8
  dataTypeLength: 255
- name: TEMP_FILE_SESSION_ID
  dataType: NUMBER
  nullable: true
  ordinalPosition: 15
  dataTypeLength:
- name: WORKSPACE_DISPLAY_NAME
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 3
  dataTypeLength: 4000
- name: WORKSPACE_ID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 1
  dataTypeLength:
- name: WORKSPACE_NAME
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: false
  ordinalPosition: 2
  dataTypeLength: 255
referencedTables:
- schema: APEX_050000
  name: WWV_FLOWS
- schema: APEX_050000
  name: WWV_FLOW_COMPANIES
- schema: APEX_050000
  name: WWV_FLOW_COMPANY_SCHEMAS
- schema: APEX_050000
  name: WWV_FLOW_FND_USER
- schema: SYS
  name: DUAL
viewDefinition: |-
  select
    w.PROVISIONING_COMPANY_ID workspace_id,
    w.short_name workspace_name,
    w.display_name workspace_display_name,
    files.id file_id,
    files.flow_id application_id,
    f.name application_name,
    nvl(files.title, files.filename) file_name,
    files.mime_type mime_type,
    files.doc_size file_size,
    files.created_by created_by,
    (
      select
        max(email_address)
      from
        wwv_flow_fnd_user
      where
        security_group_id = w.PROVISIONING_COMPANY_ID
        and user_name = files.created_by
    ) email,
    files.created_on created_on,
    decode(
      files.file_type,
      'FLOW_EXPORT',
      'Application Export',
      'IMAGE_EXPORT',
      'Text Image Export',
      'PAGE_EXPORT',
      'Application Page Export',
      'SCRIPT',
      'SQL Script',
      'THEME',
      'User Interface Theme Export',
      'XLIFF',
      'XLIFF Application Translation Export',
      files.file_type
    ) file_type,
    files.blob_content document,
    files.session_id temp_file_session_id
  from
    WWV_FLOW_FILE_OBJECTS$ files,
    wwv_flows f,
    wwv_flow_company_schemas s,
    wwv_flow_companies w,
    (
      select
        nvl(v('FLOW_SECURITY_GROUP_ID'), 0) sgid,
        (
          select
            sys_context('USERENV', 'CURRENT_USER')
          from
            sys.dual
        ) cu
      from
        sys.dual
    ) d
  where
    (
      s.schema = cu
      or (
        select
          wwv_flow_security.has_apex_administrator_role_yn(cu)
        from
          sys.dual
      ) = 'Y'
      or d.sgid = w.PROVISIONING_COMPANY_ID
    )
    and s.security_group_id = w.PROVISIONING_COMPANY_ID
    and (
      s.schema = f.owner
      or f.owner is null
    )
    and to_char(deleted_as_of, 'MM.DD.YYYY') = '01.01.0001'
    and nvl(files.file_type, 'x') not in ('CSS_FILE', 'STATIC_FILE', 'IMAGE_FILE')
    and files.flow_id = f.id(+)
    and files.security_group_id = w.PROVISIONING_COMPANY_ID
    and (
      d.sgid != 0
      or nvl(f.BUILD_STATUS, 'x') != 'RUN_AND_HIDDEN'
      or f.name is null
    )
