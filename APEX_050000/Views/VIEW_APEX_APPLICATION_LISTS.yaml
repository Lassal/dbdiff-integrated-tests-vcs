schema: APEX_050000
name: APEX_APPLICATION_LISTS
updatedAllowed: true
insertAllowed: true
columns:
- name: APPLICATION_ID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 3
  dataTypeLength:
- name: APPLICATION_NAME
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: false
  ordinalPosition: 4
  dataTypeLength: 255
- name: BUILD_OPTION
  dataType: VARCHAR2
  textMaxLength: 261
  nullable: true
  ordinalPosition: 9
  dataTypeLength: 261
- name: COMPONENT_COMMENT
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 13
  dataTypeLength: 4000
- name: COMPONENT_SIGNATURE
  dataType: VARCHAR2
  textMaxLength: 772
  nullable: true
  ordinalPosition: 15
  dataTypeLength: 772
- name: LAST_UPDATED_BY
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: true
  ordinalPosition: 11
  dataTypeLength: 255
- name: LAST_UPDATED_ON
  dataType: DATE
  nullable: true
  ordinalPosition: 12
  dataTypeLength:
- name: LIST_ENTRIES
  dataType: NUMBER
  nullable: true
  ordinalPosition: 10
  dataTypeLength:
- name: LIST_ID
  dataType: NUMBER
  nullable: false
  ordinalPosition: 14
  dataTypeLength:
- name: LIST_NAME
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: false
  ordinalPosition: 5
  dataTypeLength: 255
- name: LIST_QUERY
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 8
  dataTypeLength: 4000
- name: LIST_TYPE
  dataType: VARCHAR2
  textMaxLength: 28
  nullable: true
  ordinalPosition: 6
  dataTypeLength: 28
- name: LIST_TYPE_CODE
  dataType: VARCHAR2
  textMaxLength: 30
  nullable: true
  ordinalPosition: 7
  dataTypeLength: 30
- name: TEMPLATE
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: true
  ordinalPosition: 16
  dataTypeLength: 255
- name: WORKSPACE
  dataType: VARCHAR2
  textMaxLength: 255
  nullable: false
  ordinalPosition: 1
  dataTypeLength: 255
- name: WORKSPACE_DISPLAY_NAME
  dataType: VARCHAR2
  textMaxLength: 4000
  nullable: true
  ordinalPosition: 2
  dataTypeLength: 4000
referencedTables:
- schema: APEX_050000
  name: WWV_FLOWS
- schema: APEX_050000
  name: WWV_FLOW_COMPANIES
- schema: APEX_050000
  name: WWV_FLOW_COMPANY_SCHEMAS
- schema: APEX_050000
  name: WWV_FLOW_LISTS
- schema: APEX_050000
  name: WWV_FLOW_LIST_ITEMS
- schema: APEX_050000
  name: WWV_FLOW_PATCHES
- schema: SYS
  name: DUAL
viewDefinition: |-
  select
    w.short_name workspace,
    w.display_name workspace_display_name,
    f.ID application_id,
    f.NAME application_name,
    --
    l.NAME list_name,
    decode(
      l.list_type,
      null,
      'Static',
      'STATIC',
      'Static',
      'SQL_QUERY',
      'SQL Query',
      'FUNCTION_RETURNING_SQL_QUERY',
      'Function Returning SQL Query'
    ) list_type,
    decode(l.list_type, null, 'STATIC', l.list_type) list_type_code,
    l.list_query list_query,
    --l.LIST_STATUS                    list_status,
    --l.LIST_DISPLAYED,
    (
      select
        case
          when l.required_patch > 0 then PATCH_NAME
          else '{Not ' || PATCH_NAME || '}'
        end PATCH_NAME
      from
        wwv_flow_patches
      where
        id = abs(l.REQUIRED_PATCH)
    ) build_option,
    (
      select
        count(*)
      from
        wwv_flow_list_items
      where
        list_id = l.id
    ) list_entries,
    --
    l.LAST_UPDATED_BY last_updated_by,
    l.LAST_UPDATED_ON last_updated_on,
    l.LIST_COMMENT component_comment,
    l.ID list_id,
    --
    l.NAME || ' s=' || l.LIST_STATUS || ' bo='(
      select
        PATCH_NAME
      from
        wwv_flow_patches
      where
        id = abs(l.REQUIRED_PATCH)
    ) component_signature,
    --
    -- For compatibility with SQL Developer
    cast(null as varchar2(255)) template
  from
    wwv_flow_lists l,
    wwv_flows f,
    wwv_flow_companies w,
    wwv_flow_company_schemas s,
    (
      select
        nvl(v('FLOW_SECURITY_GROUP_ID'), 0) sgid,
        (
          select
            sys_context('USERENV', 'CURRENT_USER')
          from
            sys.dual
        ) cu
      from
        sys.dual
    ) d
  where
    (
      s.schema = cu
      or (
        select
          wwv_flow_security.has_apex_administrator_role_yn(cu)
        from
          sys.dual
      ) = 'Y'
      or d.sgid = w.PROVISIONING_COMPANY_ID
    )
    and f.security_group_id = w.PROVISIONING_COMPANY_ID
    and s.security_group_id = w.PROVISIONING_COMPANY_ID
    and s.schema = f.owner
    and f.id = l.flow_id
    and (
      d.sgid != 0
      or nvl(f.BUILD_STATUS, 'x') != 'RUN_AND_HIDDEN'
    )
    and w.PROVISIONING_COMPANY_ID != 0
