schema: APEX_050000
name: WWV_FLOW_MONTHS_MON
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  select
    cast(
      to_char(to_date(to_char(rownum, '00'), 'MM'), 'Mon') as varchar2(128)
    ) as month,
    rownum as value
  from
    wwv_flow_dual100
  where
    rownum < 13
