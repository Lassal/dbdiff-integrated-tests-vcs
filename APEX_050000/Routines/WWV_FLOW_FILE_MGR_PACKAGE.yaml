schema: APEX_050000
name: WWV_FLOW_FILE_MGR
routineType: PACKAGE
returnParamater:
  name: GET_FILE_ID.(--return--)
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 0
parameters:
- name: DOWNLOAD_FILE.P_FILE_CONTENT
  dataType: BLOB
  parameterMode: IN
  ordinalPosition: 1
- name: DOWNLOAD_FILE.P_FILE_NAME
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: DOWNLOAD_FILE.P_MIME_TYPE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 3
- name: DOWNLOAD_FILE.P_FILE_CHARSET
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 4
- name: DOWNLOAD_FILE.P_LAST_UPDATED_ON
  dataType: DATE
  parameterMode: IN
  ordinalPosition: 5
- name: DOWNLOAD_FILE.P_ETAG
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 6
- name: DOWNLOAD_FILE.P_IS_ATTACHMENT
  dataType: PL/SQL BOOLEAN
  parameterMode: IN
  ordinalPosition: 7
- name: DOWNLOAD_FILE.P_NEVER_EXPIRES
  dataType: PL/SQL BOOLEAN
  parameterMode: IN
  ordinalPosition: 8
- name: GET_FILE(1).P_ID
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 1
- name: GET_FILE(1).P_MIME_TYPE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: GET_FILE(1).P_INLINE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 3
- name: GET_FILE(2).P_FNAME
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 1
- name: GET_FILE(2).P_SECURITY_GROUP_ID
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: GET_FILE(2).P_FLOW_ID
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 3
- name: GET_FILE(2).P_INLINE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 4
- name: GET_FILE(2).P_NEVER_EXPIRES
  dataType: PL/SQL BOOLEAN
  parameterMode: IN
  ordinalPosition: 5
- name: GET_FILE(2).P_COMPATIBILITY_MODE
  dataType: PL/SQL BOOLEAN
  parameterMode: IN
  ordinalPosition: 6
- name: GET_FILE(3).P_PLUGIN_ID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_FILE(3).P_SECURITY_GROUP_ID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 2
- name: GET_FILE(3).P_FILE_NAME
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 3
- name: GET_FILE(3).P_NEVER_EXPIRES
  dataType: PL/SQL BOOLEAN
  parameterMode: IN
  ordinalPosition: 4
- name: GET_FILE(4).P_APPLICATION_ID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_FILE(4).P_THEME_NO
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 2
- name: GET_FILE(4).P_SECURITY_GROUP_ID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 3
- name: GET_FILE(4).P_FILE_NAME
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 4
- name: GET_FILE(4).P_NEVER_EXPIRES
  dataType: PL/SQL BOOLEAN
  parameterMode: IN
  ordinalPosition: 5
- name: GET_FILE(5).P_USER_ID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_FILE(5).P_CK
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: GET_FILE_ID.P_NAME
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 1
- name: GET_TEAM_FILE.P
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_TEAM_FILE.S
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 2
- name: GET_TEAM_FILE.F
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 3
- name: GET_TEAM_FILE.CK
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 4
- name: GET_WS_FILE.S
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_WS_FILE.F
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 2
- name: GET_WS_FILE.CK
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 3
routineDefinition: |-
  package wwv_flow_file_mgr as -- Copyright (c) Oracle Corporation 2000-2008. All Rights Reserved.
  --
  --    DESCRIPTION
  --      File Upload/Download services
  --
  --    SECURITY
  --
  --    NOTES
  --      Package Body should be wrapped
  --
  function get_file_id(p_name in varchar2) return number;
  --==============================================================================
  -- Procedure to download a file identified by it's id.
  -- Note: the parameter p_mime_type will be ignored and is deprecated.
  --==============================================================================
  procedure get_file(
    p_id in varchar2,
    p_mime_type in varchar2 default null,
    /* deprecated */
    p_inline in varchar2 default 'NO'
  );
  --==============================================================================
  -- Procedure to download a workspace or application file identified by its name.
  --==============================================================================
  procedure get_file(
    p_fname in varchar2,
    p_security_group_id in varchar2,
    p_flow_id in varchar2 default null,
    p_inline in varchar2 default 'YES',
    p_never_expires in boolean default false,
    p_compatibility_mode in boolean default true
  );
  --==============================================================================
  -- Procedure to download a plugin file identified by its name.
  -- If the file is a text file, all occurences of #PLUGIN_PREFIX# are replaced by
  -- the URL to access a plugin file.
  --==============================================================================
  procedure get_file (
    p_plugin_id in number,
    p_security_group_id in number,
    p_file_name in varchar2,
    p_never_expires in boolean default false
  );
  --==============================================================================
  -- Procedure to download a theme file identified by its name.
  --==============================================================================
  procedure get_file (
    p_application_id in number,
    p_theme_no in number,
    p_security_group_id in number,
    p_file_name in varchar2,
    p_never_expires in boolean default false
  );
  --==============================================================================
  -- Procedure to download a user profile image.
  --==============================================================================
  procedure get_file (
    p_user_id in number,
    p_ck in varchar2
  );
  procedure download_file (
    p_file_content in blob,
    p_file_name in varchar2,
    p_mime_type in varchar2,
    p_file_charset in varchar2,
    p_last_updated_on in date,
    p_etag in varchar2,
    p_is_attachment in boolean default true,
    p_never_expires in boolean default false
  );
  --==============================================================================
  -- Procedure to download a Websheet file identified by its ID.
  --==============================================================================
  procedure get_ws_file (
    s in number,
    -- session ID
    f in number,
    -- file id
    ck in varchar2
  );
  --==============================================================================
  -- Procedure to download a Team Development file identified by its ID.
  --==============================================================================
  procedure get_team_file (
    p in number,
    -- page ID
    s in number,
    -- session ID
    f in number,
    -- file id
    ck in varchar2
  );
  --==============================================================================
  -- Deprecated! Procedure doesn't do anything anymore.
  --==============================================================================
  procedure process_download;
  end wwv_flow_file_mgr;
