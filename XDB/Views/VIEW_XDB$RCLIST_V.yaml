schema: XDB
name: XDB$RCLIST_V
updatedAllowed: true
insertAllowed: true
columns:
- name: RCLIST
  dataType: RAW
  nullable: true
  ordinalPosition: 1
  dataTypeLength:
referencedTables:
- schema: XDB
  name: XDB$ROOT_INFO
viewDefinition: |-
  (
    select
      rclist
    from
      xdb.xdb$root_info
  )
