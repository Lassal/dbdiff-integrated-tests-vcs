schema: XDBPM
name: XDBPM_ALL_COLL_TYPES
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  select
    OWNER,
    TYPE_NAME,
    COLL_TYPE,
    UPPER_BOUND,
    ELEM_TYPE_MOD,
    ELEM_TYPE_OWNER,
    ELEM_TYPE_NAME,
    LENGTH,
    PRECISION,
    SCALE,
    CHARACTER_SET_NAME,
    ELEM_STORAGE,
    NULLS_STORED,
    CHAR_USED
  from
    ALL_COLL_TYPES
columns: []
referencedTables: []
