schema: CTXSYS
name: CTX_USER_EXTRACT_POLICIES
updatedAllowed: true
insertAllowed: true
columns:
- name: EPL_NAME
  dataType: VARCHAR2
  textMaxLength: 30
  nullable: false
  ordinalPosition: 1
  dataTypeLength: 30
referencedTables:
- schema: CTXSYS
  name: DR$INDEX
viewDefinition: |-
  select
    i.idx_name epl_name
  from
    dr$index i
  where
    INSTR(i.idx_option, 'E') > 0
    and i.idx_owner# = userenv('SCHEMAID')
