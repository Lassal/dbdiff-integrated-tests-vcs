targetObjectSchema: MDSYS
targetObjectName: SDO_COORD_REF_SYS
targetObjectType: TABLE
name: SDO_COORD_REF_SRID_UPDATE
eventTiming: AFTER EACH ROW
event: UPDATE OR DELETE
eventActionBody: |-
  BEGIN
    mdsys.mdprvt_srid.sdo_invalidate_srid_metadata(
      :old.srid,
      :old.coord_ref_sys_kind
    );
  END;
executionOrder: 1
