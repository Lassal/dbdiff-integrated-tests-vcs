schema: MDSYS
name: DBA_SDO_STYLES
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    SDO_OWNER OWNER,
    NAME,
    TYPE,
    DESCRIPTION,
    DEFINITION,
    IMAGE,
    GEOMETRY
  FROM
    SDO_STYLES_TABLE
columns: []
referencedTables: []
