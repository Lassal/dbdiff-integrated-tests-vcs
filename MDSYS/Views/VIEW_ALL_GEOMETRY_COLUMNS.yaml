schema: MDSYS
name: ALL_GEOMETRY_COLUMNS
updatedAllowed: true
insertAllowed: true
columns:
- name: COORD_DIMENSION
  dataType: NUMBER
  nullable: true
  ordinalPosition: 8
  dataTypeLength:
- name: F_GEOMETRY_COLUMN
  dataType: VARCHAR2
  textMaxLength: 64
  nullable: true
  ordinalPosition: 3
  dataTypeLength: 64
- name: F_TABLE_NAME
  dataType: VARCHAR2
  textMaxLength: 64
  nullable: true
  ordinalPosition: 2
  dataTypeLength: 64
- name: F_TABLE_SCHEMA
  dataType: VARCHAR2
  textMaxLength: 64
  nullable: true
  ordinalPosition: 1
  dataTypeLength: 64
- name: GEOMETRY_TYPE
  dataType: NUMBER
  nullable: true
  ordinalPosition: 7
  dataTypeLength:
- name: G_TABLE_NAME
  dataType: VARCHAR2
  textMaxLength: 64
  nullable: true
  ordinalPosition: 5
  dataTypeLength: 64
- name: G_TABLE_SCHEMA
  dataType: VARCHAR2
  textMaxLength: 64
  nullable: true
  ordinalPosition: 4
  dataTypeLength: 64
- name: MAX_PPR
  dataType: NUMBER
  nullable: true
  ordinalPosition: 9
  dataTypeLength:
- name: SRID
  dataType: NUMBER
  nullable: true
  ordinalPosition: 10
  dataTypeLength:
- name: STORAGE_TYPE
  dataType: NUMBER
  nullable: true
  ordinalPosition: 6
  dataTypeLength:
referencedTables:
- schema: MDSYS
  name: OGIS_GEOMETRY_COLUMNS
viewDefinition: |-
  select
    "F_TABLE_SCHEMA",
    "F_TABLE_NAME",
    "F_GEOMETRY_COLUMN",
    "G_TABLE_SCHEMA",
    "G_TABLE_NAME",
    "STORAGE_TYPE",
    "GEOMETRY_TYPE",
    "COORD_DIMENSION",
    "MAX_PPR",
    "SRID"
  from
    OGIS_GEOMETRY_COLUMNS
  where
    (
      exists (
        select
          table_name
        from
          all_tables
        where
          table_name = f_table_name
          and owner = f_table_schema
      )
      or exists (
        select
          view_name
        from
          all_views
        where
          view_name = f_table_name
          and owner = f_table_schema
      )
      or exists (
        select
          table_name
        from
          all_object_tables
        where
          table_name = f_table_name
          and owner = f_table_schema
      )
    )
    and (
      exists (
        select
          table_name
        from
          all_tables
        where
          table_name = g_table_name
          and owner = g_table_schema
      )
      or exists (
        select
          view_name
        from
          all_views
        where
          view_name = g_table_name
          and owner = g_table_schema
      )
      or exists (
        select
          table_name
        from
          all_object_tables
        where
          table_name = g_table_name
          and owner = g_table_schema
      )
    )
