schema: MDSYS
name: USER_SDO_NETWORK_FEATURE
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    NETWORK,
    FEATURE_LAYER_NAME,
    FEATURE_LAYER_ID,
    FEATURE_LAYER_TYPE,
    FEATURE_TABLE_NAME,
    RELATION_TABLE_NAME,
    HIERARCHY_TABLE_NAME
  FROM
    SDO_NETWORK_FEATURE
  WHERE
    SDO_OWNER = sys_context('USERENV', 'CURRENT_USER')
columns: []
referencedTables: []
