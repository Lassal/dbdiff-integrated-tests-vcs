schema: MDSYS
name: ALL_SDO_NETWORK_FEATURE
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    SDO_OWNER OWNER,
    NETWORK,
    FEATURE_LAYER_NAME,
    FEATURE_LAYER_ID,
    FEATURE_LAYER_TYPE,
    FEATURE_TABLE_NAME,
    RELATION_TABLE_NAME,
    HIERARCHY_TABLE_NAME
  FROM
    SDO_NETWORK_FEATURE
