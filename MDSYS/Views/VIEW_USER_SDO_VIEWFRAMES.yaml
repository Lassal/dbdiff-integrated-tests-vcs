schema: MDSYS
name: USER_SDO_VIEWFRAMES
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    NAME,
    DESCRIPTION,
    SCENE_NAME,
    DEFINITION
  FROM
    SDO_VIEWFRAMES_TABLE
  WHERE
    sdo_owner = sys_context('userenv', 'CURRENT_USER')
columns: []
referencedTables: []
