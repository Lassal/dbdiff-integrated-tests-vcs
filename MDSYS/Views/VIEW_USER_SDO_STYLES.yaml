schema: MDSYS
name: USER_SDO_STYLES
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    NAME,
    TYPE,
    DESCRIPTION,
    DEFINITION,
    IMAGE,
    GEOMETRY
  FROM
    SDO_STYLES_TABLE
  WHERE
    sdo_owner = sys_context('userenv', 'CURRENT_SCHEMA')
