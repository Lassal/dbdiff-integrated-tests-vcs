schema: MDSYS
name: ALL_SDO_VIEWFRAMES
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    SDO_OWNER OWNER,
    NAME,
    DESCRIPTION,
    SCENE_NAME,
    DEFINITION
  FROM
    SDO_VIEWFRAMES_TABLE
