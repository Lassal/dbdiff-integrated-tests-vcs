schema: MDSYS
name: USER_SDO_THEMES
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    NAME,
    DESCRIPTION,
    BASE_TABLE,
    GEOMETRY_COLUMN,
    STYLING_RULES
  FROM
    SDO_THEMES_TABLE
  WHERE
    sdo_owner = sys_context('userenv', 'CURRENT_SCHEMA')
columns: []
referencedTables: []
