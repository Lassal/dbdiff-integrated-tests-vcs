schema: MDSYS
name: USER_SDO_3DTHEMES
updatedAllowed: true
insertAllowed: true
columns: []
referencedTables: []
viewDefinition: |-
  SELECT
    NAME,
    DESCRIPTION,
    BASE_TABLE,
    THEME_COLUMN,
    STYLE_COLUMN,
    THEME_TYPE,
    DEFINITION
  FROM
    SDO_3DTHEMES_TABLE
  WHERE
    sdo_owner = sys_context('userenv', 'CURRENT_USER')
