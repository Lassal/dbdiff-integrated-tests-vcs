schema: MDSYS
name: SDO_DATUMS_OLD_FORMAT
updatedAllowed: true
insertAllowed: true
viewDefinition: |-
  SELECT
    DATUM_NAME "NAME",
    SHIFT_X,
    SHIFT_Y,
    SHIFT_Z,
    ROTATE_X,
    ROTATE_Y,
    ROTATE_Z,
    SCALE_ADJUST
  FROM
    MDSYS.SDO_DATUMS
columns: []
referencedTables: []
