schema: MDSYS
name: MDPRVT_SRID
routineType: PACKAGE
returnParamater:
  name: IS_GEODETIC.(--return--)
  dataType: VARCHAR2
  parameterMode: OUT
  ordinalPosition: 0
parameters:
- name: GET_DIMS.SRID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_REF_KIND.SRID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_UNIT(1).SRID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_UNIT(1).SRID_TYPE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: GET_UNIT(1).UNIT_ID
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 3
- name: GET_UNIT(1).UNIT_FACTOR
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 4
- name: GET_UNIT(1).UNIT_NAME
  dataType: VARCHAR2
  parameterMode: OUT
  ordinalPosition: 5
- name: GET_UNIT(2).SRID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_UNIT(2).SRID_TYPE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: GET_UNIT(2).X_UNIT_ID
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 3
- name: GET_UNIT(2).X_UNIT_FACTOR
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 4
- name: GET_UNIT(2).X_UNIT_NAME
  dataType: VARCHAR2
  parameterMode: OUT
  ordinalPosition: 5
- name: GET_UNIT(2).Y_UNIT_ID
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 6
- name: GET_UNIT(2).Y_UNIT_FACTOR
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 7
- name: GET_UNIT(2).Y_UNIT_NAME
  dataType: VARCHAR2
  parameterMode: OUT
  ordinalPosition: 8
- name: GET_UNIT(3).SRID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: GET_UNIT(3).SRID_TYPE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: GET_UNIT(3).X_UNIT_ID
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 3
- name: GET_UNIT(3).X_UNIT_FACTOR
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 4
- name: GET_UNIT(3).X_UNIT_NAME
  dataType: VARCHAR2
  parameterMode: OUT
  ordinalPosition: 5
- name: GET_UNIT(3).Y_UNIT_ID
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 6
- name: GET_UNIT(3).Y_UNIT_FACTOR
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 7
- name: GET_UNIT(3).Y_UNIT_NAME
  dataType: VARCHAR2
  parameterMode: OUT
  ordinalPosition: 8
- name: GET_UNIT(3).Z_UNIT_ID
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 9
- name: GET_UNIT(3).Z_UNIT_FACTOR
  dataType: NUMBER
  parameterMode: OUT
  ordinalPosition: 10
- name: GET_UNIT(3).Z_UNIT_NAME
  dataType: VARCHAR2
  parameterMode: OUT
  ordinalPosition: 11
- name: IS_GEODETIC.SRID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: IS_GEODETIC.SRID_TYPE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
- name: SDO_INVALIDATE_SRID_METADATA.SRID
  dataType: NUMBER
  parameterMode: IN
  ordinalPosition: 1
- name: SDO_INVALIDATE_SRID_METADATA.SRID_TYPE
  dataType: VARCHAR2
  parameterMode: IN
  ordinalPosition: 2
routineDefinition: |-
  PACKAGE mdprvt_srid AS PROCEDURE sdo_invalidate_srid_metadata(
    srid IN NUMBER,
    srid_type IN VARCHAR2 DEFAULT NULL
  );
  PRAGMA restrict_references(
    sdo_invalidate_srid_metadata,
    wnds,
    rnps,
    wnps,
    trust
  );
  FUNCTION get_unit(
    srid IN NUMBER,
    srid_type IN VARCHAR2 DEFAULT NULL,
    unit_id OUT NUMBER,
    unit_factor OUT NUMBER,
    unit_name OUT VARCHAR2
  ) -- MUST BE VARCHAR2(80)
  RETURN NUMBER DETERMINISTIC PARALLEL_ENABLE;
  FUNCTION get_unit(
    srid IN NUMBER,
    srid_type IN VARCHAR2 DEFAULT NULL,
    x_unit_id OUT NUMBER,
    x_unit_factor OUT NUMBER,
    x_unit_name OUT VARCHAR2,
    -- MUST BE VARCHAR2(80)
    y_unit_id OUT NUMBER,
    y_unit_factor OUT NUMBER,
    y_unit_name OUT VARCHAR2
  ) -- MUST BE VARCHAR2(80)
  RETURN NUMBER DETERMINISTIC PARALLEL_ENABLE;
  FUNCTION get_unit(
    srid IN NUMBER,
    srid_type IN VARCHAR2 DEFAULT NULL,
    x_unit_id OUT NUMBER,
    x_unit_factor OUT NUMBER,
    x_unit_name OUT VARCHAR2,
    -- MUST BE VARCHAR2(80)
    y_unit_id OUT NUMBER,
    y_unit_factor OUT NUMBER,
    y_unit_name OUT VARCHAR2,
    -- MUST BE VARCHAR2(80)
    z_unit_id OUT NUMBER,
    z_unit_factor OUT NUMBER,
    z_unit_name OUT VARCHAR2
  ) -- MUST BE VARCHAR2(80)
  RETURN NUMBER DETERMINISTIC PARALLEL_ENABLE;
  FUNCTION is_geodetic(
    srid IN NUMBER,
    srid_type IN VARCHAR2 DEFAULT NULL
  ) RETURN VARCHAR2 DETERMINISTIC PARALLEL_ENABLE;
  PRAGMA restrict_references(is_geodetic, wnds, rnps, wnps, trust);
  FUNCTION get_dims(srid IN NUMBER) RETURN NUMBER DETERMINISTIC PARALLEL_ENABLE;
  PRAGMA restrict_references(get_dims, wnds, rnps, wnps, trust);
  FUNCTION get_ref_kind(srid IN NUMBER) RETURN VARCHAR2 DETERMINISTIC PARALLEL_ENABLE;
  PRAGMA restrict_references(get_ref_kind, wnds, rnps, wnps, trust);
  END mdprvt_srid;
